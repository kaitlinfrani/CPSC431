/*function addItem() {
    // Get form input
    var name = document.getElementById("univname").value;
    var location = document.getElementById("location").value;
    var photo = document.getElementById("photo").value;
    var description = document.getElementById("description").value;

    // Make new univ element
    var newUniv = document.createElement("div");

    // Add univ element to list
    document.getElementById("universityList").appendChild(newUniv);

    // Reset form data
    document.getElementById("univname").value;
    document.getElementById("location").value;
    document.getElementById("photo").value;
    document.getElementById("description").value;
}

function removeItem() {
    // Remove university element from list
    document.getElementById("universityList").removeChild(button.parentNode);
    
    // Display default content if list is empty
    /*if (document.getElementById("universityList").childElementCount == 0) {
        document.getElementById("defaultContent").style.display = "block";
    }*/

/*
    // Display default form
    document.getElementById("form").style.display = "block";
    document.getElementById("form").reset();
}
*/
